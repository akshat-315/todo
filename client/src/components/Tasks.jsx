import React, { useEffect, useState } from "react";
import { useAuth } from "../context/AuthContext";

const Tasks = ({ todos }) => {
  return (
    <div className="bg-[#2D2E2D] flex-grow items-center justify-center">
      <div></div>
      <div></div>
    </div>
  );
};

export default Tasks;
